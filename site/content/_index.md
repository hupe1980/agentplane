+++
title = "agentplane"
description = "A durable, replayable, policy-governed agent runtime in Rust. Crash mid-run and resume, replay a run performing nothing, and prove afterwards what happened."
template = "index.html"

[extra]
# Marks the one page whose <title> should lead with keywords rather than with a
# page name — every other page reads better as "Security model · agentplane".
home = true
+++
