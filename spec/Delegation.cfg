\* Model for Delegation.
\*
\* Two grants give four distinct scopes — enough for a chain to narrow twice and
\* for widening to be a reachable attempt at every hop. A third grant multiplies
\* the state space without adding a failure shape: attenuation is a property of
\* subset containment, not of how many grants exist.

CONSTANTS
    Grants   = {"read", "write"}
    MaxDepth = 3

SPECIFICATION Spec

INVARIANT Safety
